name: Daily YouTube Short

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test workflow
        run: echo "YouTube Shorts automation is running!"
