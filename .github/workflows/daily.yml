name: Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Runner
        run: |
          echo "Runner is working!"
          uname -a
          python --version
